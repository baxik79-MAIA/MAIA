# M0.10 local evidence briefing verification

M0.10 adds a bounded advisory briefing path without adding execution authority.
`maia-local-briefing-host` accepts a packet only when every cited excerpt binds
to a MAIA-owned immutable Artifact in the same workspace. The Core validates
the model response before persistence. SQLite stores the canonical packet JSON,
accepted result, packet/result hashes, citations, model provenance and audit
record atomically; migration `0004_briefing_packet` is additive.

The loopback adapter only accepts a loopback socket and sends a constrained
JSON request. The recovered MAIA-owned Ollama runtime used
`qwen3:4b-instruct-2507-q4_K_M`, digest
`0edcdef34593eac1aa2be9c7d06c432dcf81945adca5eca2f27662c18f168ba0`.
The real smoke and real host/restart E2E tests passed. The latter preserved
the complete packet, accepted result and actual model after reopen, with
`execution_authority=false`.

The runtime process exposed only `127.0.0.1:11434` and had no established
non-loopback TCP connection during the observed verification. The path has no
connector, action, ApprovalGate, MemoryClaim, WorkGraph, Decision or
Commitment promotion dependency.

Validation passed: Spec Guard, generated fragments, all workspace Rust tests,
Clippy with warnings denied, 71 Python specification tests, project knowledge
export verification, and explicit real local smoke/E2E tests.
