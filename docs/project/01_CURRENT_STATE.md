# Current project state

This page is generated by `tools/update_project_knowledge.py`. The generated
section is a snapshot of live Git and canonical product metadata; rerun the
updater after a checkout, merge or milestone. It is operational documentation
and does not outrank `spec/*.yaml` or ADRs.

{{CURRENT_STATE}}

The current accepted architecture step is maintained in
`docs/project/knowledge_metadata.json`, while branch, HEAD and working-tree
facts always come from Git at update time.

This versioned file is a rendering template. Run the updater to read live facts
in `.local/project-knowledge/` or the shared mirror. Live HEAD/status are never
committed into their own commit: this avoids self-reference and preserves a
clean working tree. `--check` rejects stale rendered snapshots after a commit.
