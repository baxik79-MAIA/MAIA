# Architecture directive sources

Store exact Architecture Owner directive copies here when the source file is
available locally.

## Active

- `MAIA_Architecture_Amendment_Autonomous_Evolution_Deployment_Lock_v5.1.md`
  (v5.1, effective immediately): canonical, exact-text Architecture Owner
  directive covering the `DEVELOPMENT_EVOLUTION` / `DEPLOYMENT_LOCKED`
  capability-profile split, the Evolution Supervisor and promotion
  protocol, and protected/evolvable surfaces. Supersedes the earlier
  Human-Governed Self-Improvement directive where the two conflict. See
  `../12_DECISION_INDEX.md`.

## Related, not sourced here

- `docs/adr/ADR-0046.md`: Round Table leaves MAIA Core as an optional
  external module. This is a free-standing accepted Architecture Owner
  decision — it is not a section of v5.1 and does not derive its authority
  from it. The ADR lives under `docs/adr/`, not this directory, because it
  is not itself an exact-text directive copy.
- Capability Mesh & Decomposition Directive v1.0: additive and authoritative
  for modular decomposition/capability boundaries; ACTIVE/MANDATORY per
  Architecture Owner declaration, exact source text pending import. See
  `../12_DECISION_INDEX.md`. Compatible with, and independent of, both v5.1
  and ADR-0046 — none of the three supersedes another.

## Pending import

The M0.5 Local-First / Round Table amendment is currently pending import
from ChatGPT Library
`/MAIA/MAIA_M0.5_Architecture_Amendment_LocalFirst_RoundTable.md`; see
`../12_DECISION_INDEX.md`. This placeholder is not a reconstruction of that
source.

The earlier "Human-Governed Self-Improvement" directive referenced as
superseded by v5.1 is likewise not present as a local exact-text copy in
this repository; its content is not reconstructed here. If a source copy
becomes available, import it under this directory and update this index.
