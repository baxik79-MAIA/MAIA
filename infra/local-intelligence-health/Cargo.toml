[package]
name = "maia-local-intelligence-health"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"

[dependencies]
maia-briefing = { path = "../../core/briefing" }
maia-local-model = { path = "../local-model" }
rusqlite = { version = "0.32", features = ["bundled"] }
