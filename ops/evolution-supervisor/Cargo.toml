[package]
name = "maia-evolution-supervisor"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"

# Development-operations contract only. No binary, worker launcher, filesystem,
# Git, snapshot, process, provider, or promotion capability is linked here.
[dependencies]
